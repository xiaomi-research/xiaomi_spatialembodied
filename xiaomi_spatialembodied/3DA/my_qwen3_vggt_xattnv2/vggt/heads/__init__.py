# Packaging shim: upstream VGGT ships this directory as a namespace package.
# An explicit __init__.py keeps the vendored copy authoritative over any
# `vggt` distribution that happens to be installed in site-packages.
